// This program *calculate* the **diameter** of a cylinder's **height** in meters

let diameter  = 30;

let height  = 60;

let radius = diameter/2;

console.log(`A diameter of ${diameter} gives a radius ${radius} and a height of ${height} meters`);